module.exports = {
	printWidth: 120, // Máx ancho de línea
	trailingComma: 'all', // Comas en arrays/objetos multilínea
	singleQuote: true, // Comillas simples
	semi: true, // Punto y coma obligatorio
	tabWidth: 2, // Indentación de 2 espacios
	useTabs: true, // Espacios en lugar de tabs

	// Extras recomendados
	bracketSpacing: true, // { foo: bar } en vez de {foo:bar}
	arrowParens: 'always', // (x) => {} en vez de x => {}
	endOfLine: 'lf', // Line endings consistentes (útil en equipos multiplataforma)
};
